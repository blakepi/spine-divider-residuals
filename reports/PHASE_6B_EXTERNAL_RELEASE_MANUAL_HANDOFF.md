# Phase 6b External Release Manual Handoff

This file is an instruction draft only. Do not execute these steps without a later exact approval marker for `External Empirical Phase 6c - Authorized External Patch Release Execution`.

Current local candidate:

- Intended tag: `v1.0.1-external-summary`
- Intended title: `SPINE v1.0.1 external summary add-on`
- Scope: Machine-recoverable Harnett and Popovic summary anchors occupy distinct load-normalized regimes under source-native and bounded common-load calculations, supporting only a summary-level load-normalization hypothesis.

Required later actions, after explicit Phase 6c approval:

1. Verify the root repository and nested release mirror statuses.
2. Review the Phase 6b staging manifest, public-release manifest audit, static claim-language audit, and DOI metadata non-insertion audit.
3. Create or verify a clean nested public-release mirror commit from the staged local release files.
4. Push the nested release mirror commit to GitHub only if approved.
5. Create the approved tag only if approved.
6. Create the approved GitHub release only if approved.
7. Connect or verify the Zenodo version DOI only if approved.
8. Return the verified release URL, version DOI, concept DOI, archive checksum, and tag commit.
9. Run a separate DOI metadata insertion phase only after verified DOI values exist and explicit DOI insertion approval is present.

Phase 6b did not push, tag, create a GitHub release, attempt Zenodo, insert DOI metadata, post a preprint, or submit to a journal.
