\section{External Summary Anchor Decision Gate}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth,keepaspectratio]{FigS10_external_summary_load_sensitivity.pdf}
\caption{\textbf{Bounded summary-level load sensitivity for external Route 1 anchors.} Source-native summary anchors are plotted on the analytic divider relationship $\Gamma_{\mathrm{div}}=1/(1+\rhoload)$, and points lie on the divider by definition rather than as residual validation. The Harnett point is a model/summary anchor, not recovered paired measured per-spine data. Popovic measured summary and Popovic model-fit rows are kept separate. Class stability is shown across source-native, fixed/recovered common-load, and deterministic diagnostic-load calculations; the diagnostic grid is not a biological distribution. Threshold bars show the loads required for each row to cross low/intermediate/high $\rhoload$ classes. These rows support only a summary-level load-normalization hypothesis; no per-spine inference, biological validation, prevalence estimate, controversy resolution, or SPINE residual claim from transfer proxies is implied.}
\label{fig:external_summary_load_sensitivity}
\end{figure}
\FloatBarrier
